#微服务配置
spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: 192.168.32.128:8848
      discovery:
        namespace: dev
        group: gls-xuecheng
      config:
        namespace: dev
        group: gls-xuecheng
        file-extension: yaml
        refresh-enabled: true
        shared-configs:
          - data-id: logging-${spring.profiles.active}.yaml
            group: gls-xuecheng-common
            refresh: true
  profiles:
    active: dev