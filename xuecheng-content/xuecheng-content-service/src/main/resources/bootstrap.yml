spring:
  application:
    name: content-service
  cloud:
    nacos:
      server-addr: 192.168.32.128:8848
      discovery:
        namespace: dev
        group: gls-xuecheng
      config:
        namespace: dev
        group: gls-xuecheng
        file-extension: yaml
        refresh-enabled: true
  #profiles默认为dev
  profiles:
    active: dev


